#include <Servo.h>

Servo myServo;

void setup() {
    myServo.attach(9);  // Attach the servo to pin 9
    myServo.write(0);   // Start spinning at full speed clockwise
}

void loop() {
    // Keep running without delays
}
